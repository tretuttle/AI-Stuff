---
phase: 06-selective-invocation-and-packaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/template.md
  - .claude-plugin/plugin.json
  - .claude-plugin/marketplace.json
autonomous: true
requirements: [SELC-01, SELC-02, PLUG-02, PLUG-03]
must_haves:
  truths:
    - "A template persona file exists that users can copy to create custom personas"
    - "Plugin manifest has all marketplace-required metadata fields"
    - "Marketplace.json at .claude-plugin/ registers the persona plugin for installation"
    - "Selective invocation via --only flag works (already implemented in Phase 2)"
    - "Default behavior runs all 14 personas when no --only flag is given (already implemented in Phase 2)"
  artifacts:
    - path: "agents/template.md"
      provides: "Template persona for custom persona creation"
      contains: "name: your-persona-name"
    - path: ".claude-plugin/plugin.json"
      provides: "Marketplace-ready plugin manifest"
      contains: "repository"
    - path: ".claude-plugin/marketplace.json"
      provides: "Marketplace registry for plugin installation"
      contains: "persona"
  key_links:
    - from: "agents/template.md"
      to: "agents/theprimeagen.md"
      via: "same frontmatter fields and section structure"
      pattern: "name:|description:|tools:|disallowedTools:|memory:|model:|maxTurns:"
---

<objective>
Create the template persona file for custom persona creation, finalize plugin.json for marketplace distribution, and create marketplace.json so the plugin is installable via `/plugin install persona@ai-stuff`.

Purpose: Enables users to create custom personas and install the plugin from the tretuttle/AI-Stuff marketplace.
Output: Template persona (agents/template.md), updated plugin.json, marketplace.json
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@agents/theprimeagen.md
@.claude-plugin/plugin.json
@research/02-plugins-reference.md
@research/06-plugin-marketplaces.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create template persona and finalize plugin.json</name>
  <read_first>
    - agents/theprimeagen.md (reference structure for template)
    - .claude-plugin/plugin.json (current manifest)
    - research/02-plugins-reference.md (manifest schema)
  </read_first>
  <files>agents/template.md, .claude-plugin/plugin.json</files>
  <action>
**agents/template.md** -- Create a template persona file that users copy to create custom personas. Use the exact same structure as theprimeagen.md but with placeholder values.

The template MUST have YAML frontmatter (between triple-dash delimiters) with these fields: name: your-persona-name, description (placeholder), tools: Read Glob Grep Bash, disallowedTools: Write Edit NotebookEdit, memory: project, model: inherit, maxTurns: 10.

Body sections (each with instructions in HTML comments explaining what to fill in):
1. `# Claude Persona: [Your Persona Name]` -- opening paragraph defining who the persona is
2. `## Voice and Tone` -- how the persona communicates
3. `## Core Beliefs` -- 3-5 subsections with philosophical positions
4. `## What I Focus On` -- bullet list of review focus areas
5. `## What I Ignore` -- bullet list of things this persona deliberately skips
6. `## Project Conventions` -- COPY VERBATIM from theprimeagen.md (the CLAUDE.md reading block)
7. `## Bash Usage` -- COPY VERBATIM from theprimeagen.md
8. `## Review Output Format` -- COPY VERBATIM from theprimeagen.md (the markdown format block)
9. `## Project Stack Constraint` -- COPY VERBATIM from theprimeagen.md
10. `## Gilfoyle Mode` -- COPY VERBATIM from theprimeagen.md
11. `## JSON Output Mode` -- Template with "your-persona-name" for persona field and "Your Persona Name" for displayName
12. `## Memory Curation` -- Template with your-persona-name placeholder in the memory path

For sections 6-10, copy the exact text from theprimeagen.md since these are shared conventions all personas must follow. For sections 1-5, provide clear placeholder text with HTML comments like <!-- Replace this with your persona's voice description -->.

IMPORTANT: In the JSON Output Mode section, make sure the template JSON uses "your-persona-name" for the persona field and "Your Persona Name" for displayName, so users know to replace these.

**.claude-plugin/plugin.json** -- Update to include all marketplace metadata fields. Set version to "1.0.0" (up from 0.1.0). Add these fields: "repository": "https://github.com/tretuttle/AI-Stuff", "license": "MIT", "keywords": ["code-review", "personas", "multi-perspective", "review"]. Keep existing name, description, author, and hooks fields.
  </action>
  <verify>
    <automated>cd C:/Users/trent/AI-Stuff/persona && grep -c "your-persona-name" agents/template.md && grep -c "repository" .claude-plugin/plugin.json && grep -c "keywords" .claude-plugin/plugin.json && grep "1.0.0" .claude-plugin/plugin.json</automated>
  </verify>
  <acceptance_criteria>
    - agents/template.md exists and contains "your-persona-name" placeholder
    - agents/template.md contains all 12 sections (Voice, Core Beliefs, Focus, Ignore, Conventions, Bash, Output, Stack, Gilfoyle, JSON, Memory)
    - .claude-plugin/plugin.json contains "repository", "keywords", "license" fields
    - .claude-plugin/plugin.json version is "1.0.0"
  </acceptance_criteria>
  <done>Template persona exists with placeholder values and all 12 standard sections. Plugin manifest has full marketplace metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Create marketplace.json for plugin distribution</name>
  <read_first>
    - research/06-plugin-marketplaces.md (marketplace schema)
    - .claude-plugin/plugin.json (plugin name and metadata)
  </read_first>
  <files>.claude-plugin/marketplace.json</files>
  <action>
Create `.claude-plugin/marketplace.json` that registers the persona plugin for installation via `/plugin install persona@ai-stuff`.

The file should contain: name "ai-stuff", owner with name "Trent Tuttle", and a plugins array with one entry: name "persona", source ".", description matching plugin.json description, version "1.0.0".

The source "." means the plugin is at the root of this repository. The marketplace name "ai-stuff" matches the @ai-stuff suffix in the install command.

Also verify SELC-01 and SELC-02 are working by confirming the --only argument parsing exists in the review skill:
- Grep skills/review/SKILL.md for "--only" to confirm SELC-01 is implemented
- Grep skills/review/SKILL.md for "All 14" or "all 14" to confirm SELC-02 default behavior
  </action>
  <verify>
    <automated>cd C:/Users/trent/AI-Stuff/persona && cat .claude-plugin/marketplace.json | grep -c "persona" && grep -c "\-\-only" skills/review/SKILL.md && grep -ci "all 14" skills/review/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude-plugin/marketplace.json exists with "name": "ai-stuff" and a plugins array containing "persona"
    - marketplace.json source field is "."
    - skills/review/SKILL.md contains "--only" (SELC-01 verified)
    - skills/review/SKILL.md references all 14 personas as default (SELC-02 verified)
  </acceptance_criteria>
  <done>Marketplace.json enables `/plugin install persona@ai-stuff`. SELC-01 and SELC-02 verified as already implemented.</done>
</task>

</tasks>

<verification>
- `agents/template.md` has correct frontmatter and all 12 sections
- `.claude-plugin/plugin.json` has version 1.0.0 and marketplace metadata
- `.claude-plugin/marketplace.json` is valid JSON with persona plugin entry
- `--only` flag exists in review skill (SELC-01)
- Default all-14 behavior documented in review skill (SELC-02)
</verification>

<success_criteria>
- Template persona file enables custom persona creation
- Plugin manifest is marketplace-ready with all metadata
- Marketplace registry enables `/plugin install persona@ai-stuff`
- Selective invocation (SELC-01, SELC-02) confirmed working from Phase 2
</success_criteria>

<output>
After completion, create `.planning/phases/06-selective-invocation-and-packaging/06-01-SUMMARY.md`
</output>
