---
phase: 05-persona-memory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - memory/MEMORY-TEMPLATE.md
  - agents/theprimeagen.md
  - agents/dhh.md
  - agents/dan-abramov.md
  - agents/matt-pocock.md
  - agents/chris-coyier.md
  - agents/evan-you.md
  - agents/kent-c-dodds.md
  - agents/lee-robinson.md
  - agents/matt-mullenweg.md
  - agents/rich-harris.md
  - agents/scott-tolinski.md
  - agents/tanner-linsley.md
  - agents/theo-browne.md
  - agents/wes-bos.md
autonomous: true
requirements:
  - MEMO-01
  - MEMO-02

must_haves:
  truths:
    - "Each persona agent has memory curation instructions that prevent MEMORY.md degradation"
    - "A structured MEMORY.md template exists with sections, line limits, and curation rules"
    - "Personas know how to update, prune, and structure their memory entries"
  artifacts:
    - path: "memory/MEMORY-TEMPLATE.md"
      provides: "Canonical MEMORY.md structure template for all personas"
      contains: "Active Patterns"
    - path: "agents/theprimeagen.md"
      provides: "Memory curation section"
      contains: "Memory Curation"
    - path: "agents/dhh.md"
      provides: "Memory curation section"
      contains: "Memory Curation"
  key_links:
    - from: "agents/*.md"
      to: "memory/MEMORY-TEMPLATE.md"
      via: "curation instructions reference the template structure"
      pattern: "Active Patterns|Known Issues|Style Conventions"
---

<objective>
Add structured memory curation to all 14 persona agents so their `memory: project` MEMORY.md files stay useful over time instead of degrading into noise.

Purpose: Addresses Pitfall 7 (memory degradation). Without explicit structure and curation instructions, persona memory becomes a junk drawer of contradictory notes. Only the first 200 lines of MEMORY.md are loaded into context, so structure and pruning are critical.

Output: A MEMORY.md template file and updated system prompts for all 14 agents with memory curation instructions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@agents/theprimeagen.md
@research/04-subagents.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MEMORY.md template with structured sections and curation rules</name>
  <files>memory/MEMORY-TEMPLATE.md</files>
  <read_first>
    - research/04-subagents.md (memory scopes, 200-line auto-load limit)
    - .planning/research/PITFALLS.md (Pitfall 7 — memory degradation)
    - .planning/phases/05-persona-memory/05-CONTEXT.md (phase decisions)
  </read_first>
  <action>
Create `memory/MEMORY-TEMPLATE.md` — the canonical structure that each persona's MEMORY.md should follow. This template serves two purposes: (1) documentation of the expected structure, and (2) a starting point personas reference when creating their first memory entries.

The template MUST include these sections with line budgets (total under 200 lines to stay within auto-load limit):

```
# {Persona Name} — Project Memory

## Active Patterns (max 60 lines)
<!-- Recurring code patterns observed in this project. Update: replace outdated patterns, don't append duplicates. -->

## Known Issues (max 40 lines)
<!-- Issues seen across reviews. Remove entries when issues are fixed. -->

## Style Conventions (max 40 lines)
<!-- Project-specific style choices that override general preferences. -->

## Architecture Notes (max 30 lines)
<!-- Key architectural decisions and constraints. Prune when architecture changes. -->

## Curation Log (max 20 lines)
<!-- Track when entries were added/removed/updated. Oldest entries rotate out. -->
```

Include a comment block at the top explaining the curation rules:
- Total file MUST stay under 190 lines (buffer for the 200-line auto-load limit)
- Before adding a new insight, check if it contradicts or supersedes an existing one — remove the old entry
- Each entry should be a single concise line or short bullet (2-3 lines max)
- Entries older than 10 review sessions without reinforcement should be pruned
- The Curation Log tracks what was changed and when, rotating out oldest entries

Do NOT create individual MEMORY.md files for each persona — those are auto-created by Claude Code at `.claude/agent-memory/{agent-name}/MEMORY.md` when the agent first writes to memory.
  </action>
  <verify>
    <automated>grep -c "Active Patterns\|Known Issues\|Style Conventions\|Architecture Notes\|Curation Log" memory/MEMORY-TEMPLATE.md | grep -q "5" && echo "PASS: All 5 sections present" || echo "FAIL: Missing sections"</automated>
  </verify>
  <acceptance_criteria>
    - memory/MEMORY-TEMPLATE.md exists
    - Contains exactly 5 sections: Active Patterns, Known Issues, Style Conventions, Architecture Notes, Curation Log
    - Contains line budget comments (max 60, max 40, max 40, max 30, max 20)
    - Contains curation rules (contradict/supersede, prune, 190-line limit)
    - Total template is under 80 lines (it's a template, not filled content)
  </acceptance_criteria>
  <done>MEMORY-TEMPLATE.md exists with 5 structured sections, line budgets, and curation rules that prevent Pitfall 7 degradation</done>
</task>

<task type="auto">
  <name>Task 2: Add memory curation instructions to all 14 persona agents</name>
  <files>
    agents/theprimeagen.md, agents/dhh.md, agents/dan-abramov.md, agents/matt-pocock.md,
    agents/chris-coyier.md, agents/evan-you.md, agents/kent-c-dodds.md, agents/lee-robinson.md,
    agents/matt-mullenweg.md, agents/rich-harris.md, agents/scott-tolinski.md,
    agents/tanner-linsley.md, agents/theo-browne.md, agents/wes-bos.md
  </files>
  <read_first>
    - memory/MEMORY-TEMPLATE.md (just created in Task 1)
    - agents/theprimeagen.md (example agent structure — 8 sections)
    - agents/dan-abramov.md (second example to confirm consistent structure)
  </read_first>
  <action>
Add a "## Memory Curation" section to each of the 14 persona agent `.md` files. Insert it AFTER the "## JSON Output Mode" section (the last current section) so it doesn't disrupt existing prompt structure.

The Memory Curation section MUST contain these instructions (identical across all personas except persona name):

```markdown
## Memory Curation

You have persistent project memory at `.claude/agent-memory/{agent-name}/MEMORY.md`. The first 200 lines are auto-loaded into your context each session.

**After each review**, update your MEMORY.md with project-specific insights:

### Structure
Organize memory into these sections (stay under 190 lines total):
- **Active Patterns** (max 60 lines) — recurring code patterns in this project
- **Known Issues** (max 40 lines) — issues seen across reviews; remove when fixed
- **Style Conventions** (max 40 lines) — project-specific style choices
- **Architecture Notes** (max 30 lines) — key architectural decisions and constraints
- **Curation Log** (max 20 lines) — what you changed and when

### Rules
- Before adding an insight, check if it contradicts or supersedes an existing entry. If so, REPLACE the old entry — do not append.
- Keep each entry to 1-3 concise lines.
- If a pattern has not been reinforced in recent reviews, consider pruning it.
- Never exceed 190 lines total. If you must add and are at the limit, remove the least relevant entry first.
- Focus on insights that will change your FUTURE reviews, not summaries of past reviews.
```

Replace `{agent-name}` with the actual agent name from frontmatter (e.g., `theprimeagen`, `dhh`, `dan-abramov`).

IMPORTANT: Do not change ANY other content in the agent files. Only append the Memory Curation section at the end.
  </action>
  <verify>
    <automated>count=0; for f in agents/*.md; do grep -q "## Memory Curation" "$f" && count=$((count+1)); done; echo "$count agents have Memory Curation section"; test "$count" -eq 14 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - All 14 agent files in agents/ contain a "## Memory Curation" section
    - Each section references the correct agent name in the memory path
    - Each section contains the 5 memory structure subsections (Active Patterns, Known Issues, Style Conventions, Architecture Notes, Curation Log)
    - Each section contains the curation rules (contradict/supersede, 190-line limit, prune)
    - No other content in the agent files was modified
  </acceptance_criteria>
  <done>All 14 persona agents have Memory Curation instructions that tell them how to structure, update, and prune their MEMORY.md files to prevent degradation over time</done>
</task>

</tasks>

<verification>
1. `memory/MEMORY-TEMPLATE.md` exists with 5 structured sections and curation rules
2. All 14 files in `agents/` contain "## Memory Curation" section: `grep -l "Memory Curation" agents/*.md | wc -l` returns 14
3. Each agent references its own name in the memory path: `for f in agents/*.md; do name=$(grep "^name:" "$f" | head -1 | sed 's/name: *//'); grep -q "agent-memory/$name" "$f" && echo "OK: $name" || echo "MISSING: $name"; done`
4. No agent file lost existing sections (spot-check): `grep -c "^##" agents/theprimeagen.md` returns one more section than before
</verification>

<success_criteria>
- MEMO-01 satisfied: All 14 agents have `memory: project` (already set) AND structured curation instructions that make memory useful
- MEMO-02 satisfied: Curation rules ensure memory improves feedback relevance by focusing on project-specific patterns, pruning stale insights, and staying within the 200-line auto-load limit
- Pitfall 7 mitigated: Structured sections with line budgets prevent memory degradation into noise
</success_criteria>

<output>
After completion, create `.planning/phases/05-persona-memory/05-01-SUMMARY.md`
</output>
