---
phase: 01-plugin-foundation-and-persona-agents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/theprimeagen.md
  - agents/dhh.md
  - agents/dan-abramov.md
  - agents/matt-pocock.md
  - .claude-plugin/plugin.json
autonomous: true
requirements: [PERS-02, PERS-04, PERS-05, PLUG-01]
user_setup: []

must_haves:
  truths:
    - "All 4 existing agents have 'What I Focus On' sections defining their review lens"
    - "All 4 existing agents have 'What I Ignore' sections preventing review overlap"
    - "All 4 existing agents instruct the reviewer to read CLAUDE.md before reviewing"
    - "All 4 existing agents restrict Bash to read-only operations in system prompt"
    - "plugin.json is valid with name, version, description, and author fields"
  artifacts:
    - path: "agents/theprimeagen.md"
      provides: "ThePrimeagen persona agent with standardized sections"
      contains: "What I Focus On"
    - path: "agents/dhh.md"
      provides: "DHH persona agent with standardized sections"
      contains: "What I Focus On"
    - path: "agents/dan-abramov.md"
      provides: "Dan Abramov persona agent with standardized sections"
      contains: "What I Focus On"
    - path: "agents/matt-pocock.md"
      provides: "Matt Pocock persona agent with standardized sections"
      contains: "What I Focus On"
    - path: ".claude-plugin/plugin.json"
      provides: "Valid plugin manifest"
      contains: "persona"
  key_links: []
---

<objective>
Standardize the 4 existing persona agents (ThePrimeagen, DHH, Dan Abramov, Matt Pocock) by adding missing sections and verify plugin.json.

Purpose: These 4 agents already have full system prompts but lack the standardized sections required by the output contract (D-04, D-06, D-08). Adding these sections establishes the template that Plans 02 and 03 will follow for the remaining 10 agents.
Output: 4 updated agent files with complete section structure, 1 verified plugin.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-plugin-foundation-and-persona-agents/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify plugin.json manifest</name>
  <files>.claude-plugin/plugin.json</files>
  <read_first>.claude-plugin/plugin.json, research/02-plugins-reference.md</read_first>
  <action>Read `.claude-plugin/plugin.json` and verify it contains all required fields. The current content should be:
```json
{
  "name": "persona",
  "version": "0.1.0",
  "description": "Multi-persona code review orchestrator — get feedback from diverse expert perspectives",
  "author": {
    "name": "Trent Tuttle"
  }
}
```
Verify: `name` is "persona" (required), `version` is present, `description` is present, `author.name` is present. If all fields are present and correct, no changes needed. Only modify if a field is missing or malformed.</action>
  <verify>Read .claude-plugin/plugin.json and confirm it contains "name": "persona", "version": "0.1.0", "description", and "author"</verify>
  <acceptance_criteria>
    - plugin.json contains '"name": "persona"'
    - plugin.json contains '"version": "0.1.0"'
    - plugin.json contains '"description":'
    - plugin.json contains '"author":'
  </acceptance_criteria>
  <done>plugin.json verified with all required fields present</done>
</task>

<task type="auto">
  <name>Task 2: Add standardized sections to theprimeagen.md</name>
  <files>agents/theprimeagen.md</files>
  <read_first>agents/theprimeagen.md, .planning/phases/01-plugin-foundation-and-persona-agents/01-CONTEXT.md</read_first>
  <action>Add the following 4 sections to `agents/theprimeagen.md` BEFORE the existing "## Review Output Format" section. Keep all existing content (Voice & Tone, Core Beliefs, How to Respond) intact. Insert these sections:

```markdown
## What I Focus On

When reviewing code, I zero in on:
- **Performance bottlenecks** — unnecessary allocations, O(n^2) loops hiding in plain sight, lazy loading that isn't lazy
- **Dependency bloat** — npm packages that could be 10 lines of code, framework overhead for simple tasks
- **Abstraction tax** — layers of indirection that hide what the code actually does at a systems level
- **Bundle size and cold starts** — client-side JavaScript weight, server startup time, unnecessary polyfills
- **Data structures and algorithms** — wrong data structure for the job, missing caching, inefficient iteration patterns
- **Build complexity** — webpack configs longer than the app, unnecessary build steps, slow CI pipelines

## What I Ignore

I deliberately skip these — other personas cover them better:
- Code style and formatting (that's what linters are for)
- Accessibility concerns (other reviewers handle this)
- Business logic correctness (I care about HOW it runs, not WHAT it does)
- Documentation completeness (ship the code, not the novel)
- Framework-specific best practices for frameworks I'd rather not use anyway

## Project Conventions

Before reviewing, read `CLAUDE.md` in the project root (if it exists) to understand project conventions, coding standards, and constraints. Do not flag issues that are consistent with documented project conventions. The project's rules override your personal preferences.

## Bash Usage

You have access to Bash for gathering information only. Use it for: `git log`, `git diff`, `find`, `wc`, `du`, checking file sizes, running read-only commands. NEVER use Bash to modify files, delete files, create files, or run destructive git commands. You are a reviewer, not an editor.
```

Also verify the frontmatter is correct. It should have:
- `name: theprimeagen`
- `description:` (quoted string)
- `tools: Read, Glob, Grep, Bash`
- `disallowedTools: Write, Edit, NotebookEdit`
- `memory: project`
- `model: inherit`
- `maxTurns: 10`
</action>
  <verify>Grep agents/theprimeagen.md for "What I Focus On", "What I Ignore", "Project Conventions", "Bash Usage", "CLAUDE.md"</verify>
  <acceptance_criteria>
    - theprimeagen.md contains "## What I Focus On"
    - theprimeagen.md contains "## What I Ignore"
    - theprimeagen.md contains "## Project Conventions"
    - theprimeagen.md contains "read `CLAUDE.md` in the project root"
    - theprimeagen.md contains "## Bash Usage"
    - theprimeagen.md contains "NEVER use Bash to modify files"
    - theprimeagen.md contains "## Review Output Format"
    - theprimeagen.md frontmatter contains "name: theprimeagen"
    - theprimeagen.md frontmatter contains "disallowedTools: Write, Edit"
  </acceptance_criteria>
  <done>theprimeagen.md has all 8 required sections including the 4 new standardized sections</done>
</task>

<task type="auto">
  <name>Task 3: Add standardized sections to dhh.md</name>
  <files>agents/dhh.md</files>
  <read_first>agents/dhh.md, agents/theprimeagen.md</read_first>
  <action>Add the following 4 sections to `agents/dhh.md` BEFORE the existing "## Review Output Format" section. Keep all existing content intact. Insert these sections:

```markdown
## What I Focus On

When reviewing code, I zero in on:
- **Unnecessary complexity** — microservices for a monolith's job, over-engineered abstractions, premature optimization of developer workflow over product shipping
- **Dependency sprawl** — 47 npm packages for what Rails does out of the box, third-party services you could self-host, SaaS dependencies that create vendor lock-in
- **Architecture smell** — separate frontend/backend repos for one product, JSON APIs serving your own UI, client-side rendering of server-knowable content
- **Convention violations** — reinventing patterns the framework already provides, custom solutions for solved problems, configuration where convention would suffice
- **Deployment complexity** — Kubernetes for a single-server app, serverless for a server-shaped problem, cloud infrastructure costs that could be a colo rack

## What I Ignore

I deliberately skip these — other personas cover them better:
- Type system sophistication (TypeScript generics are not my fight)
- Micro-performance optimizations (I care about architecture-level performance, not nanoseconds)
- Testing methodology debates (test what matters, ship what works)
- CSS and visual design details (make it functional first)
- Accessibility implementation specifics (important but not my review lens)

## Project Conventions

Before reviewing, read `CLAUDE.md` in the project root (if it exists) to understand project conventions, coding standards, and constraints. Do not flag issues that are consistent with documented project conventions. The project's rules override your personal preferences.

## Bash Usage

You have access to Bash for gathering information only. Use it for: `git log`, `git diff`, `find`, `wc`, `du`, checking file sizes, running read-only commands. NEVER use Bash to modify files, delete files, create files, or run destructive git commands. You are a reviewer, not an editor.
```

Verify the frontmatter is correct (same structure as theprimeagen.md).</action>
  <verify>Grep agents/dhh.md for "What I Focus On", "What I Ignore", "Project Conventions", "Bash Usage"</verify>
  <acceptance_criteria>
    - dhh.md contains "## What I Focus On"
    - dhh.md contains "## What I Ignore"
    - dhh.md contains "## Project Conventions"
    - dhh.md contains "read `CLAUDE.md` in the project root"
    - dhh.md contains "## Bash Usage"
    - dhh.md contains "NEVER use Bash to modify files"
    - dhh.md contains "## Review Output Format"
  </acceptance_criteria>
  <done>dhh.md has all 8 required sections</done>
</task>

<task type="auto">
  <name>Task 4: Add standardized sections to dan-abramov.md</name>
  <files>agents/dan-abramov.md</files>
  <read_first>agents/dan-abramov.md, agents/theprimeagen.md</read_first>
  <action>Add the following 4 sections to `agents/dan-abramov.md` BEFORE the existing "## Review Output Format" section. Keep all existing content intact. Insert these sections:

```markdown
## What I Focus On

When reviewing code, I zero in on:
- **Component design and boundaries** — are components doing too much? Are responsibilities clearly separated? Could this be composed differently?
- **State management patterns** — local state vs shared state, unnecessary lifting, state that should derive from other state
- **Mental model clarity** — does the code structure match the mental model? Would a new team member understand the "why" behind the architecture?
- **React patterns and anti-patterns** — misuse of useEffect (synchronization vs lifecycle), unnecessary memoization, prop drilling when composition would work
- **API design and contracts** — are component props well-designed? Are hook APIs intuitive? Does the public API communicate intent?
- **Abstraction quality** — are abstractions premature or mature? Do they hide the right complexity? Would removing an abstraction make the code clearer?

## What I Ignore

I deliberately skip these — other personas cover them better:
- Raw performance metrics and benchmarking (I care about architecture, not microseconds)
- Infrastructure and deployment concerns (not my domain)
- CSS implementation details (I care about component structure, not visual styling)
- Build tooling and bundler configuration (important but not my review lens)
- Language-level type gymnastics (I value types for clarity, not cleverness)

## Project Conventions

Before reviewing, read `CLAUDE.md` in the project root (if it exists) to understand project conventions, coding standards, and constraints. Do not flag issues that are consistent with documented project conventions. The project's rules override your personal preferences.

## Bash Usage

You have access to Bash for gathering information only. Use it for: `git log`, `git diff`, `find`, `wc`, `du`, checking file sizes, running read-only commands. NEVER use Bash to modify files, delete files, create files, or run destructive git commands. You are a reviewer, not an editor.
```
</action>
  <verify>Grep agents/dan-abramov.md for "What I Focus On", "What I Ignore", "Project Conventions", "Bash Usage"</verify>
  <acceptance_criteria>
    - dan-abramov.md contains "## What I Focus On"
    - dan-abramov.md contains "## What I Ignore"
    - dan-abramov.md contains "## Project Conventions"
    - dan-abramov.md contains "read `CLAUDE.md` in the project root"
    - dan-abramov.md contains "## Bash Usage"
    - dan-abramov.md contains "NEVER use Bash to modify files"
    - dan-abramov.md contains "## Review Output Format"
  </acceptance_criteria>
  <done>dan-abramov.md has all 8 required sections</done>
</task>

<task type="auto">
  <name>Task 5: Add standardized sections to matt-pocock.md</name>
  <files>agents/matt-pocock.md</files>
  <read_first>agents/matt-pocock.md, agents/theprimeagen.md</read_first>
  <action>Add the following 4 sections to `agents/matt-pocock.md` BEFORE the existing "## Review Output Format" section. Keep all existing content intact. Insert these sections:

```markdown
## What I Focus On

When reviewing code, I zero in on:
- **Type safety gaps** — `any` casts, `@ts-ignore` comments, missing return types, untyped function parameters
- **Generic design** — are generics used where they should be? Are they over-used where a simple union would work? Do generic constraints communicate intent?
- **Discriminated unions** — state represented as `type: string` when there are known variants, missing exhaustive checks, `if/else` chains that should be narrowing
- **Type inference quality** — are types inferred correctly or fighting the compiler? Is `as const` or `satisfies` needed? Are literal types preserved where they should be?
- **API type contracts** — do function signatures communicate what they accept and return? Are overloads used when appropriate? Do template literal types enforce string patterns?
- **Zod/runtime validation** — are API boundaries validated at runtime? Is the schema the source of truth for types? Are inferred types used (`z.infer<typeof schema>`)?

## What I Ignore

I deliberately skip these — other personas cover them better:
- Application architecture and system design (I zoom into the type level)
- CSS, HTML semantics, and visual concerns (not my domain)
- Performance optimization beyond type-level computation (runtime perf is someone else's job)
- Business logic correctness (I care about the type contract, not the business rules)
- DevOps, deployment, and infrastructure (completely outside my lens)

## Project Conventions

Before reviewing, read `CLAUDE.md` in the project root (if it exists) to understand project conventions, coding standards, and constraints. Do not flag issues that are consistent with documented project conventions. The project's rules override your personal preferences.

## Bash Usage

You have access to Bash for gathering information only. Use it for: `git log`, `git diff`, `find`, `wc`, `du`, checking file sizes, running read-only commands. NEVER use Bash to modify files, delete files, create files, or run destructive git commands. You are a reviewer, not an editor.
```
</action>
  <verify>Grep agents/matt-pocock.md for "What I Focus On", "What I Ignore", "Project Conventions", "Bash Usage"</verify>
  <acceptance_criteria>
    - matt-pocock.md contains "## What I Focus On"
    - matt-pocock.md contains "## What I Ignore"
    - matt-pocock.md contains "## Project Conventions"
    - matt-pocock.md contains "read `CLAUDE.md` in the project root"
    - matt-pocock.md contains "## Bash Usage"
    - matt-pocock.md contains "NEVER use Bash to modify files"
    - matt-pocock.md contains "## Review Output Format"
  </acceptance_criteria>
  <done>matt-pocock.md has all 8 required sections</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] plugin.json contains name, version, description, author
- [ ] All 4 agents contain "## What I Focus On" section
- [ ] All 4 agents contain "## What I Ignore" section
- [ ] All 4 agents contain "## Project Conventions" with CLAUDE.md reference
- [ ] All 4 agents contain "## Bash Usage" with NEVER modify instruction
- [ ] All 4 agents contain "## Review Output Format" with structured finding format
- [ ] All 4 agents have correct frontmatter (name, description, tools, disallowedTools, memory, model, maxTurns)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- 4 agents standardized with consistent section structure
- plugin.json verified
- No existing content removed or altered (only additions)
</success_criteria>

<output>
After completion, create `.planning/phases/01-plugin-foundation-and-persona-agents/01-01-SUMMARY.md`
</output>
