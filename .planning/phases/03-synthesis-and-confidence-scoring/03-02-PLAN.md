---
phase: 03-synthesis-and-confidence-scoring
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - skills/review/SKILL.md
  - persona-reviews/sample-theprimeagen.json
  - persona-reviews/sample-dhh.json
autonomous: false
requirements: [CONF-01, SYNT-01]
must_haves:
  truths:
    - "Synthesis runs automatically at the end of /persona:review after all personas complete"
    - "Persona JSON output includes confidence scores on every finding"
    - "Sample test data exists for validating synthesis behavior"
  artifacts:
    - path: "skills/review/SKILL.md"
      provides: "Synthesis step integrated after dispatch collection"
      contains: "Synthesis Protocol"
    - path: "persona-reviews/sample-theprimeagen.json"
      provides: "Sample persona output for testing synthesis"
      contains: "theprimeagen"
    - path: "persona-reviews/sample-dhh.json"
      provides: "Sample persona output with overlapping + conflicting findings for testing"
      contains: "dhh"
  key_links:
    - from: "skills/review/SKILL.md"
      to: "skills/review/reference.md"
      via: "References Synthesis Protocol after dispatch"
      pattern: "Synthesis Protocol"
---

<objective>
Integrate synthesis into the review orchestration skill and create sample test data for validation.

Purpose: Completes the dual-invocation architecture (D-01) by adding the synthesis step to /persona:review so it runs automatically after all persona dispatches complete. Creates sample persona-review JSON fixtures with overlapping and conflicting findings for testing deduplication, disagreement detection, and confidence boosting.

Output: Updated skills/review/SKILL.md with synthesis step; sample persona-reviews/ test data; human verification checkpoint
</objective>

<execution_context>
@C:\Users\trent\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\trent\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-synthesis-and-confidence-scoring/03-CONTEXT.md
@.planning/phases/03-synthesis-and-confidence-scoring/03-RESEARCH.md
@.planning/phases/03-synthesis-and-confidence-scoring/03-01-SUMMARY.md
@skills/review/SKILL.md
@skills/review/reference.md

<interfaces>
<!-- From Plan 01: Synthesis Protocol was added to reference.md -->
<!-- The review SKILL.md just needs to reference it: "Follow the Synthesis Protocol in reference.md" -->

Current review SKILL.md structure (sections to preserve):
1. Argument Parsing (--only, --gilfoyle flags)
2. Pre-Dispatch Confirmation
3. Setup (mkdir -p persona-reviews)
4. Dispatch (Task tool parallel dispatch)
5. Collection and Summary (current simple severity listing -- this gets REPLACED)

The "Collection and Summary" section currently ends with a simple ranked severity summary.
Phase 3 replaces the post-collection summary with the full synthesis step.

JSON schema for sample test data (from reference.md):
```json
{
  "persona": "<agent-name>",
  "displayName": "<Display Name>",
  "gilfoyleMode": false,
  "target": "<review target>",
  "findings": [{ "severity", "confidence", "file", "line", "issue", "recommendation", "reasoning" }],
  "summary": "<counts>"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add synthesis step to review SKILL.md and create sample test data</name>
  <files>skills/review/SKILL.md, persona-reviews/sample-theprimeagen.json, persona-reviews/sample-dhh.json</files>
  <read_first>skills/review/SKILL.md, skills/review/reference.md, .planning/phases/03-synthesis-and-confidence-scoring/03-CONTEXT.md</read_first>
  <action>
**Part A: Update skills/review/SKILL.md**

Modify the "Collection and Summary" section. Keep the per-persona collection steps (parse JSON, write to file, print completion message). REPLACE the post-collection summary block (the simple severity listing after "After ALL personas have completed:") with a synthesis step.

The new synthesis section should appear after "After ALL personas have completed:" and replace steps 1-3 of the current post-collection block. New content:

```markdown
## Synthesis

After ALL personas have completed and their JSON files are written to `persona-reviews/`:

1. Parse `$ARGUMENTS` for `--min-confidence N` (default: 30)
2. Follow the **Synthesis Protocol** in `reference.md` to produce the unified review
3. Present the synthesized output to the user
4. Write synthesis output to `persona-reviews/SYNTHESIS.md`
```

Also update the Argument Parsing section at the top to add `--min-confidence` as a recognized flag. Add a row to the parsing examples table:

| `$ARGUMENTS` | Target | Personas | Gilfoyle | Min Confidence |
|---|---|---|---|---|
| `src/auth.ts --min-confidence 50` | src/auth.ts | All 14 | No | 50 |

Update the argument parsing description to mention: "4. **--min-confidence filter**: If `--min-confidence N` is present, use N as the minimum confidence threshold for synthesis filtering (default: 30). Critical-severity findings are never filtered regardless of this setting."

Per D-01: synthesis runs automatically at the end of /persona:review. Per D-03: produces identical output to /persona:parse-output.

Verify CONF-01: The JSON output schema in reference.md already includes `"confidence": 85` as a required integer field (0-100) on every finding. The persona agents were updated in Phase 2 to output this schema. CONF-01 is structurally satisfied by the existing schema. No persona agent modifications needed.

**Part B: Create sample test data**

Create `persona-reviews/` directory and two sample JSON files for testing synthesis. These files should exercise deduplication, disagreement detection, and confidence boosting.

`persona-reviews/sample-theprimeagen.json`:
```json
{
  "persona": "theprimeagen",
  "displayName": "ThePrimeagen",
  "gilfoyleMode": false,
  "target": "src/auth.ts",
  "findings": [
    {
      "severity": "critical",
      "confidence": 85,
      "file": "src/auth.ts",
      "line": 42,
      "issue": "Synchronous bcrypt call blocks the event loop",
      "recommendation": "Use bcrypt.hash() async variant or switch to Argon2",
      "reasoning": "This is a skill issue. You're blocking the entire event loop for password hashing. Every request queues behind this."
    },
    {
      "severity": "warning",
      "confidence": 70,
      "file": "src/auth.ts",
      "line": 15,
      "issue": "JWT secret loaded from process.env on every call",
      "recommendation": "Cache the secret at module level",
      "reasoning": "You're hitting process.env on every token verification. Read it once, cache it."
    },
    {
      "severity": "suggestion",
      "confidence": 25,
      "file": "src/auth.ts",
      "issue": "Consider adding request logging",
      "recommendation": "Add structured logging for auth requests",
      "reasoning": "Not my usual concern but having logs helps debugging."
    }
  ],
  "summary": "1 critical, 1 warning, 1 suggestion"
}
```

`persona-reviews/sample-dhh.json`:
```json
{
  "persona": "dhh",
  "displayName": "DHH",
  "gilfoyleMode": false,
  "target": "src/auth.ts",
  "findings": [
    {
      "severity": "critical",
      "confidence": 80,
      "file": "src/auth.ts",
      "line": 42,
      "issue": "Blocking bcrypt.hashSync() will tank throughput under load",
      "recommendation": "Use the async bcrypt API",
      "reasoning": "You're holding up the entire process for a CPU-bound operation. This is the kind of thing that kills production apps at 3am."
    },
    {
      "severity": "suggestion",
      "confidence": 45,
      "file": "src/auth.ts",
      "line": 15,
      "issue": "process.env reads on every request are fine",
      "recommendation": "Don't prematurely optimize -- process.env is fast enough",
      "reasoning": "Premature optimization is the root of all evil. process.env reads are negligible."
    },
    {
      "severity": "warning",
      "confidence": 65,
      "file": "src/auth.ts",
      "issue": "No rate limiting on authentication endpoint",
      "recommendation": "Add rate limiting middleware before auth routes",
      "reasoning": "Someone will brute-force this. It's not if, it's when."
    }
  ],
  "summary": "1 critical, 0 warnings, 2 suggestions"
}
```

These samples exercise:
- **Deduplication:** Both flag bcrypt sync issue in src/auth.ts:42 (should merge, boost confidence)
- **Disagreement:** ThePrimeagen says JWT env reads are a warning; DHH says they're fine (severity conflict)
- **Confidence filtering:** ThePrimeagen's logging suggestion at confidence 25 should be filtered at default threshold 30
- **CONF-01 verification:** Every finding has a confidence field with integer 0-100
  </action>
  <verify>
    <automated>grep -c "Synthesis Protocol" skills/review/SKILL.md && grep -c "min-confidence" skills/review/SKILL.md && test -f persona-reviews/sample-theprimeagen.json && test -f persona-reviews/sample-dhh.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
- grep "Synthesis Protocol" skills/review/SKILL.md returns a match
- grep "min-confidence" skills/review/SKILL.md returns a match
- grep "reference.md" skills/review/SKILL.md returns a match (synthesis reference)
- grep "SYNTHESIS.md" skills/review/SKILL.md returns a match
- The Dispatch section and per-persona collection steps are unchanged
- The simple ranked severity summary (### Critical, ### Warnings, ### Suggestions listing) is replaced by the synthesis step
- persona-reviews/sample-theprimeagen.json exists with valid JSON containing "confidence" fields
- persona-reviews/sample-dhh.json exists with valid JSON containing "confidence" fields
- Both sample files have overlapping findings for dedup testing
- Both sample files have conflicting severity for disagreement testing
  </acceptance_criteria>
  <done>Review SKILL.md has synthesis step replacing simple severity listing, --min-confidence flag in argument parsing, and references Synthesis Protocol in reference.md. Two sample JSON files exist with overlapping and conflicting findings for testing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify synthesis skill files are correct</name>
  <files>skills/review/reference.md, skills/parse-output/SKILL.md, skills/review/SKILL.md, persona-reviews/sample-theprimeagen.json, persona-reviews/sample-dhh.json</files>
  <read_first>skills/review/reference.md, skills/parse-output/SKILL.md, skills/review/SKILL.md</read_first>
  <action>
Human verifies the complete synthesis pipeline created across Plans 01 and 02. Review all three skill files for correctness, cross-references, and coherence. Verify sample test data exercises the key synthesis behaviors.
  </action>
  <what-built>Complete synthesis pipeline: Synthesis Protocol in reference.md, standalone /persona:parse-output skill, integrated synthesis step in /persona:review, and sample test data</what-built>
  <how-to-verify>
    1. Open `skills/review/reference.md` and scroll to the "Synthesis Protocol" section. Verify it contains subsections for: Input Collection, Deduplication, Confidence Boosting, Disagreement Detection, Confidence Threshold Filtering, Output Format, and Synthesis Output File.
    2. Open `skills/parse-output/SKILL.md` and verify it has valid frontmatter (name: parse-output, description, argument-hint) and references the Synthesis Protocol in reference.md.
    3. Open `skills/review/SKILL.md` and verify the argument parsing section includes --min-confidence, and the post-dispatch section references the Synthesis Protocol instead of the old simple severity listing.
    4. Open `persona-reviews/sample-theprimeagen.json` and `persona-reviews/sample-dhh.json` -- verify both have overlapping bcrypt findings (for dedup testing) and conflicting JWT env read findings (for disagreement testing).
    5. Optional end-to-end test: Run `/persona:parse-output` against the sample files to verify synthesis produces deduplicated, severity-grouped output with a Disagreements section.
  </how-to-verify>
  <verify>Human confirms all files are correct</verify>
  <acceptance_criteria>
- Human confirms Synthesis Protocol in reference.md is complete and correct
- Human confirms parse-output skill references the protocol correctly
- Human confirms review SKILL.md synthesis step replaces old summary
- Human confirms sample test data exercises dedup, disagreements, and filtering
  </acceptance_criteria>
  <done>Human approves all synthesis skill files as correct and coherent</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `skills/review/SKILL.md` contains synthesis step after dispatch
2. `skills/review/SKILL.md` argument parsing includes --min-confidence
3. Sample test data exercises deduplication, disagreement detection, and confidence filtering
4. Human verifies all three skill files are coherent and correctly cross-reference
</verification>

<success_criteria>
- /persona:review runs synthesis automatically after dispatch per D-01
- --min-confidence flag recognized in both /persona:review and /persona:parse-output per D-12/CONF-02
- Sample data validates dedup (overlapping bcrypt findings), disagreements (JWT env read severity conflict), and filtering (low-confidence logging suggestion)
- CONF-01 verified: JSON schema already includes confidence field, sample data demonstrates it
- Human approves all skill files are correct
</success_criteria>

<output>
After completion, create `.planning/phases/03-synthesis-and-confidence-scoring/03-02-SUMMARY.md`
</output>
