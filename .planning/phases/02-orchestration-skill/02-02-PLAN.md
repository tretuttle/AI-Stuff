---
phase: 02-orchestration-skill
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/theprimeagen.md
  - agents/dhh.md
  - agents/chris-coyier.md
  - agents/dan-abramov.md
  - agents/evan-you.md
  - agents/kent-c-dodds.md
  - agents/lee-robinson.md
  - agents/matt-mullenweg.md
  - agents/matt-pocock.md
  - agents/rich-harris.md
  - agents/scott-tolinski.md
  - agents/tanner-linsley.md
  - agents/theo-browne.md
  - agents/wes-bos.md
autonomous: true
requirements:
  - ORCH-03
  - ORCH-04

must_haves:
  truths:
    - "Every persona agent respects the project's stack choices as non-negotiable foundational decisions"
    - "Every persona agent can output structured JSON when dispatched by the orchestrator"
    - "Every persona agent activates Gilfoyle mode when instructed"
    - "Persona agents remain read-only (Write/Edit still disallowed)"
  artifacts:
    - path: "agents/theprimeagen.md"
      provides: "Updated persona with base constraint, JSON output, Gilfoyle support"
      contains: "non-negotiable"
    - path: "agents/dhh.md"
      provides: "Updated persona with base constraint, JSON output, Gilfoyle support"
      contains: "non-negotiable"
  key_links:
    - from: "agents/*.md"
      to: "skills/review/reference.md"
      via: "JSON output schema conformance"
      pattern: "persona.*displayName.*findings"
    - from: "agents/*.md"
      to: "skills/review/SKILL.md"
      via: "Task tool dispatch by agent name"
      pattern: "name:"
---

<objective>
Update all 14 persona agent files to support orchestrator dispatch: add the base project-stack constraint (D-14), Gilfoyle mode awareness (D-13-D-16), and JSON output capability for file-based collection (D-06).

Purpose: Personas need three new capabilities to work with the orchestration skill -- respecting project stack choices, outputting JSON when dispatched, and supporting the Gilfoyle mode flag.
Output: All 14 agents/*.md files updated with 3 new sections
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-orchestration-skill/02-CONTEXT.md
@.planning/phases/02-orchestration-skill/02-RESEARCH.md
</context>

<interfaces>
<!-- Current persona agent structure (8 sections). New sections will be added after existing content. -->

From agents/theprimeagen.md (representative of all 14 agents):
```yaml
---
name: theprimeagen
description: "Performance-obsessed systems engineer..."
tools: Read, Glob, Grep, Bash
disallowedTools: Write, Edit, NotebookEdit
memory: project
model: inherit
maxTurns: 10
---
```

Sections in current agents (in order):
1. Voice & Tone
2. Core Beliefs
3. How to Respond
4. What I Focus On
5. What I Ignore
6. Project Conventions
7. Bash Usage
8. Review Output Format

New sections to add (after existing section 8):
9. Project Stack Constraint (D-14)
10. Gilfoyle Mode (D-13, D-15, D-16)
11. JSON Output Mode (D-06)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update first 7 persona agents with orchestrator support sections</name>
  <files>agents/theprimeagen.md, agents/dhh.md, agents/chris-coyier.md, agents/dan-abramov.md, agents/evan-you.md, agents/kent-c-dodds.md, agents/lee-robinson.md</files>
  <read_first>
    agents/theprimeagen.md (full file -- understand current structure and "Review Output Format" section placement)
    agents/dhh.md (verify consistent structure across agents)
    agents/chris-coyier.md (verify consistent structure)
    .planning/phases/02-orchestration-skill/02-CONTEXT.md (D-14, D-13, D-15, D-16 exact wording)
    .planning/phases/02-orchestration-skill/02-RESEARCH.md (JSON schema for output)
  </read_first>
  <action>
For each of the 7 agent files, append three new sections AFTER the existing "Review Output Format" section. Do NOT modify any existing content. Do NOT change frontmatter (especially do NOT add Write to tools or remove it from disallowedTools -- personas stay read-only per PERS-04).

**Section 9: Project Stack Constraint** (per D-14 -- always active, not just Gilfoyle mode)
Add this exact section to each agent:

```markdown
## Project Stack Constraint

Before reviewing, identify the project's technology stack from CLAUDE.md, package.json, and the codebase itself. These technology choices are NON-NEGOTIABLE foundational decisions. You MUST treat them as settled.

You CAN critique:
- How the stack is being used (bad patterns, misuse of APIs, missing features)
- Implementation quality within the chosen technologies
- Configuration and setup issues

You MUST NOT recommend:
- Replacing core technologies (e.g., "switch from React to Svelte")
- Removing foundational dependencies (e.g., "drop Next.js and use Vite")
- Adopting a fundamentally different architecture pattern

Roast the implementation, not the architecture.
```

**Section 10: Gilfoyle Mode** (per D-13, D-15, D-16)
Add this exact section to each agent:

```markdown
## Gilfoyle Mode

When your dispatch prompt includes a "GILFOYLE MODE ACTIVE" section, activate maximum-intensity review:

- Drop all diplomacy. Hold nothing back.
- Your strongest opinions on web development are cranked to maximum.
- Be brutal about bad patterns, missed opportunities, wrong abstractions, and anti-patterns.
- Stay within the project's existing architecture (see Project Stack Constraint above).
- Your confidence scores should reflect your genuine conviction, not politeness.
- If something is bad, say it is bad. If something is terrible, say it is terrible.

When Gilfoyle mode is NOT active, maintain your natural voice and tone. You can still be opinionated, but with your usual level of diplomacy.
```

**Section 11: JSON Output Mode** (per D-06, supporting ORCH-04)
Add this exact section to each agent:

```markdown
## JSON Output Mode

When your dispatch prompt asks you to return findings as JSON, output ONLY a valid JSON object (no markdown fencing, no commentary before or after) with this exact structure:

```json
{
  "persona": "{your-agent-name}",
  "displayName": "{Your Display Name}",
  "gilfoyleMode": false,
  "target": "{review-target}",
  "findings": [
    {
      "severity": "critical | warning | suggestion",
      "confidence": 85,
      "file": "path/to/file.ts",
      "line": 42,
      "issue": "Description of the issue",
      "recommendation": "What to do instead",
      "reasoning": "Why this matters -- in your voice"
    }
  ],
  "summary": "N critical, N warnings, N suggestions"
}
```

Set `gilfoyleMode` to `true` if Gilfoyle mode was activated for this review. The `line` field is optional -- omit it if not applicable. Keep findings focused: 1-5 findings per review, prioritizing the most impactful issues.

When JSON output is NOT requested (e.g., direct invocation), use the markdown Review Output Format above.
```

For each of the 7 agents, fill in the persona-specific values in the JSON template:
- theprimeagen: persona="theprimeagen", displayName="ThePrimeagen"
- dhh: persona="dhh", displayName="DHH"
- chris-coyier: persona="chris-coyier", displayName="Chris Coyier"
- dan-abramov: persona="dan-abramov", displayName="Dan Abramov"
- evan-you: persona="evan-you", displayName="Evan You"
- kent-c-dodds: persona="kent-c-dodds", displayName="Kent C. Dodds"
- lee-robinson: persona="lee-robinson", displayName="Lee Robinson"
  </action>
  <verify>
    <automated>for f in theprimeagen dhh chris-coyier dan-abramov evan-you kent-c-dodds lee-robinson; do echo "=== $f ===" && grep -c "NON-NEGOTIABLE" "agents/$f.md" && grep -c "GILFOYLE MODE" "agents/$f.md" && grep -c "JSON Output Mode" "agents/$f.md"; done</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 7 agent files contains "## Project Stack Constraint" section
    - Each contains the string "NON-NEGOTIABLE foundational decisions"
    - Each contains "Roast the implementation, not the architecture"
    - Each contains "## Gilfoyle Mode" section
    - Each contains "GILFOYLE MODE ACTIVE" as the activation trigger
    - Each contains "## JSON Output Mode" section
    - Each contains the JSON schema with fields: persona, displayName, gilfoyleMode, target, findings, summary
    - Each finding schema includes: severity, confidence, file, issue, recommendation, reasoning
    - Frontmatter is UNCHANGED -- `disallowedTools` still contains `Write, Edit, NotebookEdit`
    - Existing sections 1-8 are UNCHANGED
    - theprimeagen.md JSON example uses persona="theprimeagen", displayName="ThePrimeagen"
    - dhh.md JSON example uses persona="dhh", displayName="DHH"
  </acceptance_criteria>
  <done>First 7 persona agents updated with Project Stack Constraint, Gilfoyle Mode, and JSON Output Mode sections</done>
</task>

<task type="auto">
  <name>Task 2: Update remaining 7 persona agents with orchestrator support sections</name>
  <files>agents/matt-mullenweg.md, agents/matt-pocock.md, agents/rich-harris.md, agents/scott-tolinski.md, agents/tanner-linsley.md, agents/theo-browne.md, agents/wes-bos.md</files>
  <read_first>
    agents/theprimeagen.md (after Task 1 update -- use as reference for new section placement and format)
    agents/matt-mullenweg.md (verify current structure before editing)
    agents/matt-pocock.md (verify current structure)
    agents/rich-harris.md (verify current structure)
    agents/scott-tolinski.md (verify current structure)
    agents/tanner-linsley.md (verify current structure)
    agents/theo-browne.md (verify current structure)
    agents/wes-bos.md (verify current structure)
  </read_first>
  <action>
Apply the same three sections as Task 1 to the remaining 7 agents. Append AFTER the existing "Review Output Format" section. Do NOT modify existing content or frontmatter.

Add the identical three sections (Project Stack Constraint, Gilfoyle Mode, JSON Output Mode) using the exact same text as Task 1, with persona-specific values in the JSON template:

- matt-mullenweg: persona="matt-mullenweg", displayName="Matt Mullenweg"
- matt-pocock: persona="matt-pocock", displayName="Matt Pocock"
- rich-harris: persona="rich-harris", displayName="Rich Harris"
- scott-tolinski: persona="scott-tolinski", displayName="Scott Tolinski"
- tanner-linsley: persona="tanner-linsley", displayName="Tanner Linsley"
- theo-browne: persona="theo-browne", displayName="Theo Browne"
- wes-bos: persona="wes-bos", displayName="Wes Bos"

Each agent file gets the exact same section text as defined in Task 1, only with the `persona` and `displayName` values swapped to match the specific agent.
  </action>
  <verify>
    <automated>for f in matt-mullenweg matt-pocock rich-harris scott-tolinski tanner-linsley theo-browne wes-bos; do echo "=== $f ===" && grep -c "NON-NEGOTIABLE" "agents/$f.md" && grep -c "GILFOYLE MODE" "agents/$f.md" && grep -c "JSON Output Mode" "agents/$f.md"; done</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 7 agent files contains "## Project Stack Constraint" section
    - Each contains "NON-NEGOTIABLE foundational decisions"
    - Each contains "Roast the implementation, not the architecture"
    - Each contains "## Gilfoyle Mode" section with "GILFOYLE MODE ACTIVE" trigger
    - Each contains "## JSON Output Mode" section with JSON schema
    - Frontmatter is UNCHANGED in all 7 files
    - Existing sections 1-8 are UNCHANGED in all 7 files
    - matt-pocock.md JSON example uses persona="matt-pocock", displayName="Matt Pocock"
    - rich-harris.md JSON example uses persona="rich-harris", displayName="Rich Harris"
    - theo-browne.md JSON example uses persona="theo-browne", displayName="Theo Browne"
  </acceptance_criteria>
  <done>All 14 persona agents now have Project Stack Constraint, Gilfoyle Mode, and JSON Output Mode sections</done>
</task>

</tasks>

<verification>
- All 14 agents contain "NON-NEGOTIABLE foundational decisions" (D-14)
- All 14 agents contain "GILFOYLE MODE ACTIVE" trigger text (D-13)
- All 14 agents contain "JSON Output Mode" section (D-06)
- All 14 agents still have `disallowedTools: Write, Edit, NotebookEdit` in frontmatter (PERS-04)
- No existing persona content was modified (sections 1-8 intact)
</verification>

<success_criteria>
- Every persona agent has the base project-stack constraint (D-14)
- Every persona agent recognizes and activates Gilfoyle mode (D-13, D-15, D-16)
- Every persona agent can output structured JSON matching the schema in reference.md (D-06)
- Persona agents remain read-only -- no Write/Edit tools (PERS-04)
- JSON output includes persona name, display name, gilfoyle flag, target, findings array, and summary
</success_criteria>

<output>
After completion, create `.planning/phases/02-orchestration-skill/02-02-SUMMARY.md`
</output>
